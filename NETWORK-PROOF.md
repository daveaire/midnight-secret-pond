# Local Midnight proof evidence

On September 19, 2026, Secret Pond Proof was deployed to the repository's isolated Midnight devnet and a real `proveOpportunity` transaction was generated by proof-server `8.1.0`, submitted through Midnight.js `4.1.1`, finalized, and read back from the indexer.

## Finalized public evidence

| Field | Value |
| --- | --- |
| Network | Local `undeployed` Midnight devnet |
| Contract address | `c405e092783bcb13b938576b193c088b1fcd4dabaf213d24b3e3c4cf8868a3ab` |
| Transaction ID | `0034a0eea184e1fd613b514a18d351440ac189eca25e97da1abf2e75e40b05de30` |
| Block height | `32` |
| Accepted-proof count | `1` |
| Public commitment | `85d57bee58f1ac06a4d985c68a8b5d0d103e8be91f46247ec93bb89d5d9fc171` |

The transaction used synthetic opportunity data. The route label, net-return input, bridge-time input, and liquidity input stayed in the encrypted local private-state store and were not printed or placed in public ledger state.

## Reproduce

With Docker Desktop and the Compact compiler installed:

```sh
npm install
npm run network:setup
npm run network:prove -- --route synthetic-hackathon-route-1 \
  --net-bps 80 --bridge-seconds 600 --liquidity-usd 25000
```

`network:setup` starts the pinned local node, indexer, and proof server, compiles the contract, and deploys it. `network:prove` synchronizes the wallet, generates the proof, submits the transaction, waits for finalization, queries the indexed contract state, and prints only public results.

This transaction belongs to a disposable local devnet, so its identifiers are reproducible evidence rather than a permanent public explorer record. A Preview or Preprod deployment is the remaining step for independently accessible network evidence.
